---
phase: 01-foundation
plan: 2
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/agent/db.py
  - backend/agent/redaction.py
  - backend/tests/test_redaction.py
  - backend/.env.example
  - backend/scripts/verify_db.py
autonomous: false
requirements: [FOUND-01, FOUND-02]
user_setup:
  - service: supabase
    why: "Persistence backbone for memory, RAG, and traces; the connection layer needs a live project"
    env_vars:
      - name: SUPABASE_POOLER_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Transaction Pooler (port 6543)"
      - name: SUPABASE_DIRECT_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Direct connection (port 5432)"
      - name: CRON_SECRET
        source: "Generate a random 32-char string; set identically in local .env and Vercel env (used by Plan 04 keepalive)"
    dashboard_config:
      - task: "Create (or confirm) the Supabase free-tier project; pgvector is pre-installed and enabled by the Plan 03 migration"
        location: "Supabase Dashboard"
      - task: "Set SUPABASE_POOLER_URL, SUPABASE_DIRECT_URL, CRON_SECRET as environment variables"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables (and local backend/.env)"
must_haves:
  truths:
    - "pooler_connection() opens a connection to Supabase port 6543 and runs SELECT NOW() without InvalidSqlStatementName or too-many-clients errors"
    - "The values of SUPABASE_POOLER_URL and SUPABASE_DIRECT_URL are redacted from log output"
    - "SUPABASE_POOLER_URL, SUPABASE_DIRECT_URL, and CRON_SECRET are documented in backend/.env.example"
  artifacts:
    - path: backend/agent/db.py
      provides: "Async connection factory: pooler_connection() (6543, app queries) and direct_connection() (5432, migrations only)"
      exports: ["pooler_connection", "direct_connection"]
      contains: "prepare_threshold=None"
    - path: backend/agent/redaction.py
      provides: "Redaction coverage for Supabase connection-string values"
      contains: "SUPABASE"
    - path: backend/scripts/verify_db.py
      provides: "One-shot pooler smoke (SELECT NOW())"
  key_links:
    - from: backend/agent/db.py
      to: "Supabase Transaction Pooler (port 6543)"
      via: "psycopg.AsyncConnection.connect with prepare_threshold=None, autocommit=True, row_factory=dict_row"
      pattern: "prepare_threshold=None"
---

<objective>
Build the shared database connection layer (FOUND-02) and provision the live Supabase
project (FOUND-01) so the backend can run a real query against the Transaction Pooler. Close
the secret-redaction gap the connection strings introduce, document the new env vars, and
prove the connection end-to-end with a smoke query.

Purpose: Every later phase (Memory checkpointer, RAG chunks, Observability traces) imports
this connection factory. Getting the pooler kwargs (`prepare_threshold=None`,
`autocommit=True`, `row_factory=dict_row`) and the per-request lifecycle right here is what
makes all of them work on Supabase Supavisor transaction mode.
Output: `backend/agent/db.py`, a redaction fix, env-var documentation, a smoke script, and
a verified live `SELECT NOW()` over the pooler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@backend/agent/redaction.py
</context>

<artifacts_this_plan_produces>
- `backend/agent/db.py` (NEW) — `pooler_connection()`, `direct_connection()`, `_pooler_url()`, `_direct_url()`
- `backend/agent/redaction.py` (EDIT) — `SECRET_ENV_MARKERS` gains `"SUPABASE"`
- `backend/tests/test_redaction.py` (EDIT) — case asserting a Supabase URL value is redacted
- `backend/.env.example` (EDIT, via Bash append) — `SUPABASE_POOLER_URL`, `SUPABASE_DIRECT_URL`, `CRON_SECRET`
- `backend/scripts/verify_db.py` (NEW) — async pooler smoke
- Live: provisioned Supabase project + env vars wired in Vercel and local `.env`
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create db.py connection factory, close the redaction gap, document env vars</name>
  <files>backend/agent/db.py, backend/agent/redaction.py, backend/tests/test_redaction.py, backend/.env.example</files>
  <read_first>
    - backend/agent/redaction.py (the file being modified — note SECRET_ENV_MARKERS on line 10 and configured_secret_values on lines 17-24)
    - backend/tests/test_redaction.py (the test file being modified)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Pattern 1: psycopg3 Pooler Connection Factory" + Pitfall 2 + "Security Domain")
    - .planning/phases/01-foundation/01-PATTERNS.md ("backend/agent/db.py" + "Secret Redaction Coverage" + "Env Var Access + RuntimeError on Missing")
  </read_first>
  <action>
    Create `backend/agent/db.py` mirroring `redaction.py` module conventions: start with
    `from __future__ import annotations`; private `_pooler_url()` / `_direct_url()` helpers
    that read `SUPABASE_POOLER_URL` / `SUPABASE_DIRECT_URL` from `os.environ` and raise
    RuntimeError at CALL time (not import time) when unset. Expose two
    `@asynccontextmanager` async generators: `pooler_connection()` connecting to the pooler
    URL with `prepare_threshold=None` (required — Supavisor transaction mode; omitting it
    causes psycopg InvalidSqlStatementName per Pitfall 2), `autocommit=True`, and
    `row_factory=dict_row` (both required for the Phase-2 AsyncPostgresSaver); and
    `direct_connection()` connecting to the direct URL with `autocommit=True`,
    `row_factory=dict_row`, documented as migrations-only. No module-level connection
    singleton — connections are per-call (Vercel ephemeral). Use the exact factory shape in
    RESEARCH "Pattern 1".

    Close the redaction gap: `configured_secret_values()` in `redaction.py` builds its
    secret set from env-var NAMES matching `SECRET_ENV_MARKERS`. `SUPABASE_POOLER_URL` and
    `SUPABASE_DIRECT_URL` contain none of the current markers (KEY/TOKEN/SECRET/PASSWORD/
    CREDENTIAL), so their values — which embed the DB password inline — would NOT be
    redacted. Add `"SUPABASE"` to the `SECRET_ENV_MARKERS` tuple so any `SUPABASE_*` value
    is scrubbed. This is the only change to redaction.py. (`CRON_SECRET` is already covered
    by the existing `SECRET` marker.)

    Add a test to `backend/tests/test_redaction.py`: set a fake `SUPABASE_POOLER_URL`
    env value via patch, call `redact_secrets` on a string embedding that value, and assert
    the fake password/host substring does not survive (the value is replaced by the
    redaction marker). Follow the existing test class style in that file.

    Append to `backend/.env.example` (NOTE: this file is deny-listed for the Edit tool —
    append via Bash, e.g. a heredoc `>>`) the three documented vars with the comment block
    from RESEARCH "Environment Variables (add to .env.example)": SUPABASE_POOLER_URL (6543,
    all app queries), SUPABASE_DIRECT_URL (5432, migrations only), CRON_SECRET.
  </action>
  <verify>
    <automated>cd backend && python -c "import agent.db as d; assert hasattr(d,'pooler_connection') and hasattr(d,'direct_connection'); print('db.py OK')" && python -m unittest tests.test_redaction -v && grep -ac "SUPABASE_POOLER_URL" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `backend/agent/db.py` imports cleanly and exports `pooler_connection` and `direct_connection`
    - `db.py` passes `prepare_threshold=None`, `autocommit=True`, `row_factory=dict_row` to the pooler connection
    - `redaction.py` `SECRET_ENV_MARKERS` includes `"SUPABASE"`
    - `python -m unittest tests.test_redaction -v` passes, including the new Supabase-URL redaction case
    - `backend/.env.example` contains `SUPABASE_POOLER_URL`, `SUPABASE_DIRECT_URL`, and `CRON_SECRET`
  </acceptance_criteria>
  <done>The connection factory exists with correct pooler kwargs, Supabase connection-string values are provably redacted, and the new env vars are documented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Provision Supabase and wire connection strings + CRON_SECRET (blocking)</name>
  <files>backend/.env.example</files>
  <action>Pause for human-supplied inputs: create/confirm the Supabase project and set SUPABASE_POOLER_URL, SUPABASE_DIRECT_URL, and CRON_SECRET in local .env and Vercel env (see how-to-verify). The smoke test (Task 3) cannot run until these exist.</action>
  <what-built>The connection layer and env-var documentation are in place but require a live Supabase project and real connection strings that only you can supply from the dashboard.</what-built>
  <how-to-verify>
    1. In the Supabase dashboard, create a free-tier project (or confirm the existing one). pgvector is pre-installed; the Plan 03 migration enables it — no manual extension step needed now.
    2. Copy the Transaction Pooler connection string (port 6543) into `SUPABASE_POOLER_URL` and the Direct connection string (port 5432) into `SUPABASE_DIRECT_URL`.
    3. Generate a random 32-char `CRON_SECRET`.
    4. Set all three in your local `backend/.env` AND in the Vercel project's Environment Variables (Settings -> Environment Variables). Do NOT commit real values; `.env` is gitignored.
    5. Confirm `LANGCHAIN_TRACING_V2` is NOT set in Vercel env (RESEARCH Security Domain — no external trace backend).
  </how-to-verify>
  <resume-signal>Type "env set" once SUPABASE_POOLER_URL, SUPABASE_DIRECT_URL, and CRON_SECRET are populated locally and in Vercel.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Smoke-test the live pooler connection (SELECT NOW())</name>
  <files>backend/scripts/verify_db.py</files>
  <read_first>
    - backend/agent/db.py (created in Task 1 — the factory under test)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Validation Architecture" -> "Observable Validation" item 1; "psycopg3 Connection with Prepared Statements Disabled")
  </read_first>
  <action>
    Create `backend/scripts/verify_db.py`: a tiny async smoke that opens
    `pooler_connection()` from `agent.db`, executes `SELECT NOW()`, prints the returned
    timestamp, and exits 0 on success / non-zero on any exception. Use `asyncio.run(...)`.
    This is the FOUND-02 / Success-Criterion-1 proof: a real query over port 6543. It is a
    reusable smoke, not throwaway. Run it from `backend/` with the env vars set in Task 2.

    If the run raises `InvalidSqlStatementName`, the pooler kwargs are wrong (missing
    `prepare_threshold=None`) — fix db.py, do not work around it in the script. If it raises
    a connection/auth error, the env strings are wrong — surface that to the user, do not
    hardcode a fallback.
  </action>
  <verify>
    <automated>cd backend && python scripts/verify_db.py</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/verify_db.py` exits 0 and prints a current timestamp
    - no `psycopg.errors.InvalidSqlStatementName` and no "too many clients" error occurs
    - the connection used port 6543 (the pooler URL), not 5432
  </acceptance_criteria>
  <done>The backend connects to Supabase via the Transaction Pooler and runs a real query without connection errors — Roadmap Success Criterion 1 met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend process → Supabase Postgres | DB credentials (connection string with password) cross into the app environment |
| backend env → logs / exceptions | Secret values may be emitted in log lines or stack traces |
| backend env → frontend bundle | Vite exposes only `VITE_`-prefixed vars; secrets must never carry that prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `SUPABASE_POOLER_URL` / `SUPABASE_DIRECT_URL` values (embed DB password) leaking via logs/exceptions | high | mitigate | Task 1 adds `"SUPABASE"` to `SECRET_ENV_MARKERS` so these values are redacted globally; Task 1 test asserts redaction; `_pooler_url()` raises a generic RuntimeError (no value in message) |
| T-01-03 | Elevation of Privilege | Supabase connection string / service-role key exposed to the frontend bundle | high | mitigate | Connection strings are backend-only, read from `os.environ` in `db.py`; not `VITE_`-prefixed, so Vite never bundles them; no DB credential is sent to the client |
| T-01-04 | Denial of Service | Connection exhaustion if app queries use the direct URL (port 5432, 60-conn limit) | medium | mitigate | `pooler_connection()` (6543) is the app path; `direct_connection()` is documented and used migrations-only (Plan 03) |
| T-01-OBS | Information Disclosure | External trace SaaS (LangSmith) silently shipping data | medium | mitigate | Checkpoint step 5 confirms `LANGCHAIN_TRACING_V2` is unset in Vercel env |
</threat_model>

<verification>
- `python -c "import agent.db"` imports without error; factory exports present.
- `python -m unittest tests.test_redaction -v` green, including the Supabase-URL case.
- `backend/.env.example` documents the three new vars.
- `python scripts/verify_db.py` returns a live timestamp over the pooler.
</verification>

<success_criteria>
FOUND-02 satisfied: a shared DB connection layer uses the Transaction Pooler (6543, prepared
statements disabled) for queries and the direct connection (5432) for migrations only.
FOUND-01 partially satisfied: Supabase project provisioned and env vars wired (pgvector
enabling lands in Plan 03's migration). Roadmap Success Criterion 1 met. Secret redaction
invariant preserved for the new connection strings.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done — record the db.py public
surface and confirm the smoke query returned a timestamp.
</output>

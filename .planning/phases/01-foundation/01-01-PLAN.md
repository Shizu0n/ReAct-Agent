---
phase: 01-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
autonomous: false
requirements: [FOUND-05]
must_haves:
  truths:
    - "The full backend unit suite (5 modules) passes on langgraph >=1.2.6 with zero regressions"
    - "AsyncPostgresSaver imports cleanly from langgraph.checkpoint.postgres.aio"
    - "pip resolves the upgraded langchain ecosystem without dependency conflicts"
  artifacts:
    - path: backend/requirements.txt
      provides: "Upgraded LangGraph ecosystem pins + new Phase 1 DB packages"
      contains: "langgraph-checkpoint-postgres"
  key_links:
    - from: backend/requirements.txt
      to: "installed venv"
      via: "pip install -r requirements.txt resolves the compatible set"
      pattern: "langgraph>=1\\.2"
---

<objective>
Upgrade the LangChain/LangGraph ecosystem to the version set required by
`langgraph-checkpoint-postgres>=3.1.0`, install the new Phase 1 database packages
(`psycopg[binary]`, `psycopg-pool`), and EMPIRICALLY confirm compatibility by running the
existing unit suite. This is the hard blocker for every downstream phase (FOUND-05) and
absorbs the "LangGraph upgrade spike" the roadmap calls for.

Purpose: The current pins (langgraph 0.2.45 / langchain-core 0.3.x) are incompatible with
the Postgres checkpoint stack every later phase imports. The upgrade is 0.2.45 → 1.2.6 —
nearly the whole ecosystem moves in one step. RESEARCH rates this MEDIUM confidence: the
APIs this codebase uses are stable, but only a real test run proves it. A test regression
is a STOP condition, not a warning.
Output: An upgraded, conflict-free `backend/requirements.txt` and a green unit suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@backend/requirements.txt
</context>

<artifacts_this_plan_produces>
- `backend/requirements.txt` — exact `==` pins on the langchain ecosystem replaced with `>=` minimums; new packages `langgraph-checkpoint-postgres>=3.1.0`, `psycopg[binary]>=3.2.0`, `psycopg-pool>=3.2.0`; explicit `langgraph-checkpoint` and `langgraph-sdk` pins removed (resolved transitively); `numpy==1.26.4` and all non-langchain pins preserved.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the langchain-ecosystem pins in requirements.txt</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt (the file being modified)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Standard Stack" + "Installation (requirements.txt changes)" + Pitfall 4)
    - .planning/phases/01-foundation/01-PATTERNS.md ("backend/requirements.txt (config edit)")
  </read_first>
  <action>
    ENCODING HAZARD — `backend/requirements.txt` is UTF-16 LE with a BOM and CRLF line
    endings (verified: first bytes FF FE). The Edit tool may corrupt it. Rewrite the file
    preserving UTF-16-LE-with-BOM + CRLF (e.g. write the new content with that encoding),
    OR re-encode to UTF-8 only if you confirm pip still installs cleanly in Task 3; do not
    silently leave it half-converted.

    Change every LangChain-ecosystem line from an exact `==` pin to a `>=` minimum, using
    the RESEARCH target set: langchain>=1.3.11, langchain-community>=0.4.2,
    langchain-core>=1.4.8, langchain-text-splitters>=1.1.2, langgraph>=1.2.6,
    langsmith>=0.9.3. REMOVE the explicit `langgraph-checkpoint==2.1.2` and
    `langgraph-sdk==0.1.74` lines entirely — they are resolved transitively and explicit
    pins create conflicts (RESEARCH Pitfall 4). ADD three new packages:
    langgraph-checkpoint-postgres>=3.1.0, psycopg[binary]>=3.2.0, psycopg-pool>=3.2.0.

    Do NOT touch numpy==1.26.4 (Python 3.13 wheel constraint per CLAUDE.md) or any other
    non-langchain pin — those stay exactly as they are. This is a surgical pin change, not
    a full regeneration: only the langchain-ecosystem lines and the three additions change.
  </action>
  <verify>
    <automated>cd backend && grep -aoE "langgraph(-checkpoint-postgres)?>=[0-9.]+" requirements.txt && grep -aoE "psycopg(\[binary\]|-pool)>=[0-9.]+" requirements.txt && grep -ac "numpy==1.26.4" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains `langgraph>=1.2.6`, `langchain-core>=1.4.8`, and `langgraph-checkpoint-postgres>=3.1.0`
    - requirements.txt contains `psycopg[binary]>=3.2.0` and `psycopg-pool>=3.2.0`
    - requirements.txt no longer contains `langgraph-checkpoint==` or `langgraph-sdk==`
    - `numpy==1.26.4` is still present and unchanged
    - the file is still readable by pip (parses as a valid requirements file in Task 3)
  </acceptance_criteria>
  <done>The langchain ecosystem is upgraded to `>=` minimums and the three new DB packages are added, with numpy and all other pins untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify net-new package legitimacy on PyPI (blocking)</name>
  <files>backend/requirements.txt</files>
  <action>Pause before install. Confirm the three net-new packages on pypi.org against their official orgs/repos (see how-to-verify). This gate is never auto-approvable.</action>
  <what-built>requirements.txt now declares three net-new packages that RESEARCH's Package Legitimacy Audit flagged `SUS (unknown downloads)` — a false positive caused by the audit tool being unable to query PyPI download counts, not by any malicious signal. Each is from a known org with a documented source repo.</what-built>
  <how-to-verify>
    Before installing, confirm legitimacy of the net-new packages on the official registry:
    1. https://pypi.org/project/psycopg/ — published by "The Psycopg Team" (psycopg.org), THE PostgreSQL Python adapter.
    2. https://pypi.org/project/psycopg-pool/ — companion package, same org.
    3. https://pypi.org/project/langgraph-checkpoint-postgres/ — sub-library of github.com/langchain-ai/langgraph (official LangChain AI).
    The upgraded langchain/langgraph packages are already trusted (in-project version bumps).
    See RESEARCH.md "Package Legitimacy Audit" for the full verdict table.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install upgraded deps and run the full unit suite (hard regression gate)</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt (post-Task-1 state)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Upgrade procedure for the plan" + "LangGraph Upgrade: Breaking-Change Analysis")
    - backend/tests/test_agent.py, backend/tests/test_api.py (the suite that must stay green)
  </read_first>
  <action>
    From `backend/`, install the upgraded requirements into the project venv (reuse the
    existing `backend/.venv`, or create a fresh Python 3.11/3.12 venv — Python 3.14 has no
    numpy 1.26.4 wheel). If `pip install -r requirements.txt` aborts with a dependency
    conflict, that is the expected failure mode for a stale transitive pin — read the
    conflicting package from pip's output and relax/remove its explicit pin (RESEARCH
    Open Question 2 notes `langchain-community` pulling `langchain-classic` as the most
    likely culprit); do not pin around it blindly.

    Once installed, run the FULL suite from `backend/`: `python -m unittest discover -s tests -v`
    (all 5 modules: test_agent, test_api, test_llms, test_redaction, test_suggestions).
    Also confirm the Phase-2 import path resolves now:
    `python -c "from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver"`.

    STOP CONDITION: if ANY test fails, do NOT proceed and do NOT weaken the assertion to
    make it pass — the existing 100% baseline must hold on the new runtime. Diagnose the
    failure (almost certainly a transitive import, not a StateGraph API change per RESEARCH)
    and report it. A green suite is the gate that unblocks Plans 02-04.
  </action>
  <verify>
    <automated>cd backend && python -m unittest discover -s tests -v && python -c "from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver; print('AsyncPostgresSaver import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pip install -r requirements.txt` completes with no unresolved dependency conflict
    - `python -m unittest discover -s tests -v` exits 0 with every test marked OK across all 5 modules
    - `from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver` imports without error
    - installed `langgraph` version is >= 1.2.6 (`pip show langgraph`)
  </acceptance_criteria>
  <done>The upgraded ecosystem is installed, the Postgres checkpoint import resolves, and the existing unit suite is fully green on langgraph >=1.2.6 — FOUND-05 met and downstream plans unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build environment | Third-party package code is downloaded and installed into the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs of `psycopg`, `psycopg-pool`, `langgraph-checkpoint-postgres` | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 2 checkpoint) verifies each on pypi.org against its official org/repo before install; RESEARCH audit confirms SUS = false positive (unqueryable download counts), not malware |
| T-01-UP | Tampering | Upgraded langchain ecosystem behaving differently than 0.2.x | low | mitigate | Full existing unit suite is the empirical regression gate; any behavior change fails a test and STOPs the plan |
</threat_model>

<verification>
- `pip install -r requirements.txt` resolves cleanly (no conflict abort).
- `python -m unittest discover -s tests -v` is fully green (5 modules, 0 failures, 0 errors).
- `from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver` succeeds.
- numpy and all non-langchain pins are unchanged in the diff.
</verification>

<success_criteria>
FOUND-05 satisfied: LangGraph is upgraded to a version compatible with
`langgraph-checkpoint-postgres` (and forward-compatible with `langchain-mcp-adapters` per
RESEARCH), and all existing backend unit tests pass with no regressions. Roadmap Success
Criterion 4 met. Downstream persistence plans (02-04) are unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done — record the resolved
package versions (`pip freeze` of the langchain/psycopg lines) so later phases pin against
the confirmed set, and note any transitive pin that had to be relaxed.
</output>

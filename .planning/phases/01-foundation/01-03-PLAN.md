---
phase: 01-foundation
plan: 3
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .planning/phases/01-foundation/migration.sql
  - backend/scripts/verify_schema.py
autonomous: false
requirements: [FOUND-01, FOUND-03]
user_setup:
  - service: supabase
    why: "The migration must be APPLIED to the live database; this needs the provisioned project + direct connection"
    env_vars:
      - name: SUPABASE_DIRECT_URL
        source: "Already wired in Plan 02 (Supabase Dashboard -> Database -> Direct connection, port 5432)"
must_haves:
  truths:
    - "documents, document_chunks, traces, and keepalive tables exist in the live Supabase public schema after applying the migration"
    - "chunks_embedding_hnsw_idx HNSW index exists on document_chunks.embedding"
    - "the pgvector extension is enabled and the keepalive table holds exactly one row (id=1)"
  artifacts:
    - path: .planning/phases/01-foundation/migration.sql
      provides: "Idempotent DDL: pgvector extension, 4 tables, HNSW index, keepalive seed row"
      contains: "chunks_embedding_hnsw_idx"
    - path: backend/scripts/verify_schema.py
      provides: "Asserts the 4 tables + HNSW index exist in live Supabase"
  key_links:
    - from: .planning/phases/01-foundation/migration.sql
      to: "live Supabase public schema (port 5432 direct connection)"
      via: "applied via Supabase MCP apply_migration / supabase db push / psql against SUPABASE_DIRECT_URL"
      pattern: "USING hnsw"
---

<objective>
Author the idempotent schema migration (FOUND-03), APPLY it to the live Supabase database
via the direct connection (port 5432), and VERIFY — as a step distinct from applying — that
the four tables plus the HNSW vector index actually exist in Supabase.

Purpose: Authoring `migration.sql` is NOT the same as the schema existing. Roadmap Success
Criterion 2 requires the tables and index to EXIST in the live database. Apply and verify
are kept separate: a build/import check passing does not prove the schema is live.
Output: `migration.sql`, applied DDL in Supabase, and a verification script confirming the
4 tables + HNSW index.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<artifacts_this_plan_produces>
- `.planning/phases/01-foundation/migration.sql` (NEW) — `CREATE EXTENSION IF NOT EXISTS vector`; `documents`, `document_chunks` (with `embedding vector(768)`), `traces`, `keepalive` tables; supporting btree indexes; `chunks_embedding_hnsw_idx` HNSW index (`vector_cosine_ops`, m=16, ef_construction=64); `keepalive` seeded with `id=1`.
- `backend/scripts/verify_schema.py` (NEW) — asserts the 4 tables + HNSW index exist.
- Live Supabase DB objects: the 4 tables + HNSW index + pgvector extension.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author migration.sql</name>
  <files>.planning/phases/01-foundation/migration.sql</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md ("Schema Migration SQL" section — the verbatim SQL and the "Notes on schema decisions")
    - .planning/phases/01-foundation/01-PATTERNS.md ("migration.sql" — key constraints)
  </read_first>
  <action>
    Create `.planning/phases/01-foundation/migration.sql` using the exact DDL from the
    RESEARCH "Schema Migration SQL" section. It must: enable pgvector
    (CREATE EXTENSION IF NOT EXISTS vector); create `documents`, `document_chunks`,
    `traces`, `keepalive` with CREATE TABLE IF NOT EXISTS; type `document_chunks.embedding`
    as vector(768) (gemini-embedding-001 truncated to 768 dims per RAG-02); make
    `document_chunks.document_id` an FK to documents(id) ON DELETE CASCADE; create the
    supporting btree indexes; create `chunks_embedding_hnsw_idx` over
    document_chunks.embedding USING hnsw with vector_cosine_ops and WITH (m = 16,
    ef_construction = 64); and seed `keepalive` with a single id=1 row via
    INSERT ... ON CONFLICT (id) DO NOTHING plus the singleton unique index.

    Every statement uses IF NOT EXISTS (idempotent — safe to re-run). Do NOT create any
    LangGraph checkpoint/store tables — Phase 2 calls AsyncPostgresSaver.setup() to manage
    its own schema (RESEARCH note). This file is migration DDL, not app code.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS" .planning/phases/01-foundation/migration.sql && grep -c "USING hnsw" .planning/phases/01-foundation/migration.sql && grep -c "CREATE EXTENSION IF NOT EXISTS vector" .planning/phases/01-foundation/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - migration.sql declares exactly the four tables documents, document_chunks, traces, keepalive
    - it contains CREATE EXTENSION IF NOT EXISTS vector
    - it contains the HNSW index chunks_embedding_hnsw_idx with vector_cosine_ops, m=16, ef_construction=64
    - document_chunks.embedding is typed vector(768)
    - every CREATE statement is guarded with IF NOT EXISTS; no checkpoint tables are created
  </acceptance_criteria>
  <done>The idempotent migration SQL exists with all four tables, the pgvector extension, and the HNSW index.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration to the live Supabase database (BLOCKING schema push)</name>
  <files>.planning/phases/01-foundation/migration.sql</files>
  <read_first>
    - .planning/phases/01-foundation/migration.sql (authored in Task 1 — the DDL to apply)
    - .planning/phases/01-foundation/01-RESEARCH.md ("Architectural Responsibility Map" -> Schema migration; Pitfall 5; Open Question 1)
  </read_first>
  <action>
    Apply migration.sql to the LIVE Supabase database over the DIRECT connection (port
    5432), NOT the pooler. Use whichever is available: the Supabase MCP apply_migration tool
    (preferred — a linked project is connected), `supabase db push`, or
    psql against $SUPABASE_DIRECT_URL with the migration file.

    This is the explicit schema push: authoring the file (Task 1) does NOT make the tables
    exist — this task does. If no project is linked and SUPABASE_DIRECT_URL is unset, STOP
    and request the direct connection string from the user (it was wired in Plan 02). The
    migration is additive and idempotent, so re-running over a project that already has some
    objects (e.g. pre-existing checkpoint tables per Open Question 1) is safe. Do not log the
    connection string.
  </action>
  <verify>
    <automated>echo "Apply step — schema existence is asserted by Task 3 (verify_schema.py). Confirm the apply command returned success."</automated>
  </verify>
  <acceptance_criteria>
    - the migration was executed against the direct (5432) connection / linked project without error
    - the apply tooling reported success (no SQL error)
    - this step did NOT use the pooler (6543)
  </acceptance_criteria>
  <done>The migration DDL has been applied to the live Supabase database.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the schema is live (4 tables + HNSW index) — distinct from apply</name>
  <files>backend/scripts/verify_schema.py</files>
  <action>Create and run backend/scripts/verify_schema.py to assert the 4 tables + HNSW index exist in the live DB, then visually confirm in the Supabase dashboard (see how-to-verify). This is the verify step, kept separate from the apply (Task 2).</action>
  <what-built>migration.sql has been applied to the live Supabase project. This step verifies — independently of the apply — that the schema is actually live, and asks you to visually confirm in the dashboard.</what-built>
  <how-to-verify>
    1. Create `backend/scripts/verify_schema.py`: an async script using `direct_connection()` (or `pooler_connection()`) from `agent.db` that runs
       `SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('documents','document_chunks','traces','keepalive')`
       (must return 4 rows) and
       `SELECT indexname FROM pg_indexes WHERE indexname='chunks_embedding_hnsw_idx'`
       (must return 1 row); exit 0 only if both hold, non-zero otherwise. Run it: `cd backend && python scripts/verify_schema.py` — it must exit 0 printing "4 tables, hnsw index present".
    2. In the Supabase dashboard Table Editor, visually confirm `documents`, `document_chunks`, `traces`, `keepalive` are listed, and that `keepalive` has exactly one row (id=1).
  </how-to-verify>
  <resume-signal>Type "schema verified" once verify_schema.py exits 0 and all four tables are visible in the dashboard.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live database (DDL) | Schema-changing SQL is executed against production Postgres |
| direct connection (5432) | Privileged migration path, limited connection budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | Schema injection / destructive DDL via migration.sql | low | mitigate | Migration runs outside app code (no user input reaches it), is authored from RESEARCH, and is idempotent (IF NOT EXISTS — re-runs do not drop or overwrite) |
| T-01-04 | Denial of Service | Migration over direct (5432) exhausting the 60-connection budget | medium | mitigate | One-shot apply over a single direct connection; app queries never use 5432 (pooler only) |
| T-01-DD | Tampering | Accidental drop of pre-existing data (e.g. checkpoint tables) | low | accept | All statements are additive CREATE ... IF NOT EXISTS; no DROP/ALTER; re-run is safe per RESEARCH Open Question 1 |
</threat_model>

<verification>
- migration.sql contains the 4 tables, pgvector extension, and HNSW index (grep gates, Task 1).
- The migration is applied to the live DB over the direct connection (Task 2).
- `verify_schema.py` exits 0: pg_tables returns 4 target tables and chunks_embedding_hnsw_idx exists (Task 3).
- Dashboard visual confirmation of the four tables + single keepalive row (Task 3).
</verification>

<success_criteria>
FOUND-03 satisfied: the schema migration creates the documents, document_chunks, traces, and
keepalive tables plus the HNSW vector index, and they EXIST in the live Supabase database.
FOUND-01 completed: pgvector is enabled. Roadmap Success Criterion 2 met.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done — record how the migration
was applied (MCP / CLI / psql) and paste the verify_schema.py output.
</output>
